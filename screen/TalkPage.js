import { View, Text, StyleSheet } from "react-native";

export const TalkPage = () => {
  return (
    <View style={styles.talkPage}>
      <Text>TalkPage</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  talkPage: { alignItems: "center", width: "100%" },
});
